language: node_js
node_js:
  - "6"
  - "4"
cache:
  npm: false
script:
  - npm run build
  - npm test
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master